<%= form_with(model: given_answer, local: true) do |form| %>
  <% if given_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(given_answer.errors.count, "error") %> prohibited this given_answer from being saved:</h2>

      <ul>
      <% given_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :given_answer_user_id %>
  </div>

  <div class="field">
    <%= form.label :answer %>
    <%= form.text_field :answer, id: :given_answer_answer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
